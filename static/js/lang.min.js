(function(e) {
    function n(e) {
        if (!e) return;
        if (e === "") return;
        $(".lang-selector a").removeClass("active");
        $(".lang-selector a[data-language-name='" + e + "']").addClass("active");
        for (var n = 0; n < t.length; n++) {
            $(".highlight." + t[n]).hide()
        }
        $(".highlight." + e).show();
       // $(window.location.hash).get(0).scrollIntoView(true)
    }

    function r(e) {
        if (!history) {
            return
        }
        var t = window.location.hash;
        if (t) {
            t = t.replace(/^#+/, "")
        } 

        history.pushState({}, "", "?" + e + "#" + t);
        localStorage.setItem("language", e)
    }

    function i(e) {
        var r = e[0];
        var i = localStorage.getItem("language");
        t = e;
        if (location.search.substr(1) !== "" && jQuery.inArray(location.search.substr(1), t) != -1) {
            n(location.search.substr(1));
            localStorage.setItem("language", location.search.substr(1))
        } else if (i !== null && jQuery.inArray(i, t) != -1) {
            n(i)
        } else {
            n(t[0])
        }
    }
    var t = [];
    e.setupLanguages = i;
    e.activateLanguage = n;


    $(function() {
        $(".lang-selector a").on("click", function() {
            var languageName = $(this).data("language-name");
            //r(languageName);
            n(languageName);
            return false
        });
        window.onpopstate = function(e) {
            n(window.location.search.substr(1))
        }
    })

})(window)
